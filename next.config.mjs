/** @type {import('next').NextConfig} */
const nextConfig = {
	reactStrictMode: true,
	images: {
		domains: ['plus.unsplash.com', 'images.unsplash.com', 'www.apple.com'],
	},
};

export default nextConfig;
